#pragma once
#include <string>
#include <iostream>
#include "page.h"
#include <fstream>
using namespace std;
string header();
string header(string);
string para();
string para(string);
page scanHTML(string);
